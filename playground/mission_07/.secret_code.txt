SECRET CODE PIECE #7 OF 12
===========================

Code word: DETECTIVE

You automated the mission! You used a script with variables
to generate 5 personalized case assignments in seconds.

THE SCRIPT TOOLKIT:
  #!/bin/bash                — the shebang: tells Mac to use bash
  NAME="Agent CIPHER"        — create a variable (NO spaces around =)
  echo "Hello $NAME"         — use a variable with $
  TODAY=$(date +"%B %d")     — capture command output into a variable
  $((5 + 3))                 — do math
  read ANSWER                — get input from the user
  chmod +x script.sh         — make script executable
  bash script.sh             — run it

REMEMBER:
  Double quotes "..." expand variables: "Hello $NAME" → "Hello CIPHER"
  Single quotes '...' do NOT: 'Hello $NAME' → 'Hello $NAME'

Current progress: 7 / 12 code pieces found.
