SECRET CODE PIECE #8 OF 12
===========================

Code word: CONGRATULATIONS

You automated a batch operation with loops!
What would take hours manually took seconds with a loop.

THE LOOP TOOLKIT:
  for item in a b c; do ... done          — loop over a list
  for i in {1..10}; do ... done           — loop over numbers
  for file in folder/*.txt; do ... done   — loop over files
  while [ condition ]; do ... done        — loop while true

CONDITIONALS:
  if [ -f file ]; then ... fi             — does file exist?
  if [ -d folder ]; then ... fi           — does folder exist?
  if [ $x -gt 5 ]; then ... fi           — is x greater than 5?
  if [ "$str" = "hello" ]; then ... fi    — string comparison

USEFUL NUMBER TRICKS:
  printf "%03d" 7    — prints: 007 (zero-padded)
  $((counter + 1))   — arithmetic

Current progress: 8 / 12 code pieces found.
