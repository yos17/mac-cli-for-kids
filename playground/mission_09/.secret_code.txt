SECRET CODE PIECE #9 OF 12
===========================

Code word: YOU

(Yes, YOU again — the message is almost complete!)

You talked to the internet directly from Terminal!

THE NETWORK TOOLKIT:
  ping hostname              — check if a server is reachable
  ping -c 4 hostname         — send exactly 4 pings then stop
  curl url                   — fetch a URL and show the response
  curl -I url                — show HTTP headers only
  curl -s url                — silent mode (no progress bar)
  curl -s -o /dev/null -w "%{http_code}" url  — just show status code
  open url                   — open URL in default browser
  open .                     — open current folder in Finder
  caffeinate                 — prevent Mac from sleeping (Ctrl+C to stop)

HTTP STATUS CODES:
  200 — OK (success)
  301 — Redirect (page moved)
  404 — Not Found
  403 — Forbidden (you don't have permission)
  500 — Server Error

Current progress: 9 / 12 code pieces found.
