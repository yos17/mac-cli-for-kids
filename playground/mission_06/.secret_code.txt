SECRET CODE PIECE #6 OF 12
===========================

Code word: TERMINAL

You analyzed real data with pipes and supercommands!

THE PIPE TOOLKIT:
  cmd1 | cmd2              — pipe output of cmd1 into cmd2
  sort file.txt            — sort lines alphabetically
  sort -n file.txt         — sort numerically (not alphabetically!)
  sort -r file.txt         — sort in reverse
  uniq file.txt            — remove consecutive duplicates
  sort | uniq -c           — count how many times each line appears
  sort | uniq -c | sort -rn — most common first!
  cut -d',' -f1 file.csv   — extract column 1 (comma-separated)
  cut -d',' -f2 file.csv   — extract column 2

POWERFUL COMBOS:
  cat access_log.csv | cut -d',' -f4 | sort | uniq -c | sort -rn
  (counts how many times each status code appears)

  cat suspects_database.csv | cut -d',' -f2 | sort | uniq -c
  (counts suspects by city)

Current progress: 6 / 12 code pieces found.
